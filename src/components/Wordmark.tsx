// The fixed brand wordmark in the top-left chrome (hidden on small screens).

export function Wordmark() {
  return (
    <div className="cc-chrome cc-wordmark">
      <span className="dot" />
      <span className="gm">Cringe Cemetery</span>
    </div>
  );
}
